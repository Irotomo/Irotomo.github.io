# 이로토모 사이트 — 설치와 운영

## 파일 구성

| 파일 | 하는 일 |
|---|---|
| `index.html` | 메인 랜딩 페이지 |
| `board.html` | 중고 · 나눔 게시판 |
| `admin.html` | **관리자 페이지 — 메인 페이지 문구와 모임 날짜를 고치는 곳** |
| `Code.gs` | 위 세 페이지가 공통으로 쓰는 서버 코드 (구글 시트에 붙임) |

**셋 다 하나의 구글 시트, 하나의 웹앱 주소를 같이 씁니다.** 시트 안에서 탭만 나뉩니다.

- `settings` 탭 → 메인 페이지 문구와 모임 날짜
- `posts` 탭 → 게시판 글

그래서 설치는 아래 한 번이면 세 페이지가 전부 살아납니다.

---

# 설치 (한 번만)

## 1. 구글 시트 만들기

[sheets.new](https://sheets.new) 에서 새 시트를 만들고 이름을 `이로토모` 정도로 지정합니다. 탭 이름은 건드리지 않아도 됩니다. 스크립트가 `settings` 와 `posts` 를 알아서 만듭니다.

## 2. Apps Script 붙이기

1. 시트 메뉴에서 **확장 프로그램 → Apps Script**
2. 열려 있는 `Code.gs` 를 모두 지우고 이 폴더의 `Code.gs` 내용을 붙여넣습니다.
3. 맨 위 설정을 확인합니다.

   | 항목 | 기본값 | 설명 |
   |---|---|---|
   | `ADMIN_ID` | `admin` | 관리자 페이지 아이디 |
   | `ADMIN_PW` | `3707` | 관리자 비번. 게시판 글 강제 삭제에도 씁니다 |
   | `SALT` | — | 아무 문자열로 바꾸세요 (비밀번호 해싱용) |
   | `NOTIFY_TO` | `irotomokor@gmail.com` | 게시판에 새 글이 올라오면 알림 받을 주소 |

4. 저장합니다.

## 3. 웹앱으로 배포

1. 오른쪽 위 **배포 → 새 배포**
2. 톱니바퀴 → **웹 앱**
3. **다음 사용자로 실행: 나** / **액세스 권한이 있는 사용자: 모든 사용자**
4. 배포를 누르면 권한 승인 창이 뜹니다. 계정 선택 → 고급 → 안전하지 않은 페이지로 이동 → 허용
5. 나온 웹 앱 URL 을 복사합니다.

## 4. 주소 확인

`index.html`, `board.html`, `admin.html` 세 파일 모두에 현재 주소가 이미 들어가 있습니다. 각 파일에서 `var API = ` 로 시작하는 줄을 찾아 값이 3번에서 복사한 주소와 같은지만 확인하시면 됩니다. 다르면 **세 파일 모두** 새 주소로 바꿔야 합니다.

## 5. GitHub 에 올리기

`Code.gs` 를 뺀 세 개(`index.html`, `board.html`, `admin.html`)를 같은 폴더에 올리면 끝입니다.

---

# 관리자 페이지

## 접속

```
https://<계정명>.github.io/<저장소명>/admin.html
```

주소를 직접 입력해서 들어갑니다. 사이트 어디에도 링크가 없고 검색엔진 수집도 막아뒀습니다. 휴대폰 홈 화면에 추가해 두시면 편합니다.

아이디 `admin` / 비밀번호 `3707`

## 메인 페이지 문구 수정

**메인 페이지(index.html)에 실린 글자 75개를 한국어·일본어 각각 고칠 수 있습니다.** 탭으로 구역이 나뉩니다.

| 탭 | 항목 수 | 내용 |
|---|---|---|
| 상단 메뉴 | 8 | 로고 옆 이름, 메뉴 링크, 신청 버튼 |
| 첫 화면 | 5 | 큰 제목, 소개 문단, 버튼 두 개 |
| 요약 카드 | 7 | 장소 · 참가비 · 신청 칸 (일시는 «일정» 탭에서) |
| 모임 소개 | 8 | 섹션 제목과 본문, 카드 3개 |
| 당일 흐름 | 5 | 15시~18시 진행 안내 |
| 참가비 | 9 | 금액, 예약금 안내 |
| 오시는 길 | 6 | 가게 이름, 주소, 교통편 |
| 방침 | 6 | 지켜주실 점 |
| FAQ | 7 | 질문과 답변 |
| 게시판 안내 | 4 | 메인 하단의 게시판 소개 띠 |
| 가나 연습 | 4 | 가나 쓰기 연습 안내 |
| 신청 | 2 | 신청 안내 문구 |
| 제휴 | 4 | 제휴 · 스폰서 문의, 푸터 |
| **일정** | — | 모임 날짜와 시간 |

기본값에서 달라진 항목에는 금색 테두리가 생기고 탭에도 점이 붙습니다. 「기본값으로」를 누르면 원래 문구로 돌아갑니다.

## 모임 날짜

«일정» 탭에서 두 가지 중 고릅니다.

- **매주 일요일** — "매주 일요일"로 표시되고, 일본어 신청 폼에는 다가오는 일요일 12주치가 자동으로 들어갑니다.
- **지정한 날짜만** — 달력에서 날짜를 하나씩 담습니다. 요약 카드에는 다가오는 날짜 3개까지 보이고, 신청 폼의 참가일 선택지도 이 날짜들로 바뀝니다. 지난 날짜는 자동으로 빠지니 목록에서 지우지 않아도 됩니다.

시간도 같은 탭에서 고칩니다.

## 어떻게 반영되나

저장하면 시트의 `settings` 탭에 쌓입니다. **기본값과 똑같은 항목은 저장하지 않으므로**, 세 군데만 고쳤다면 시트에도 세 줄만 남습니다. 시트에서 그 줄을 지우면 파일에 적힌 원래 문구로 돌아갑니다.

메인 페이지는 열릴 때 이 설정을 한 번 읽어서 덮어씁니다. 못 읽어오더라도 파일에 적힌 문구가 그대로 보이므로 화면이 비는 일은 없습니다.

## 관리자 페이지로 못 고치는 것

- 일본어 신청 폼의 입력칸 이름 (お名前, 国籍 등) → `index.html` 직접 수정
- 게시판에 올라온 글 → 아래 참고
- 색 · 글꼴 · 배치 → CSS 이므로 파일 수정

---

# 게시판 운영

## 글 숨기기 · 지우기

시트 `posts` 탭에서 해당 줄의 **`status` 열**을 `open` → `hidden` 으로 바꾸면 즉시 목록에서 사라집니다. 사이트에서 지우려면 글 상세 화면의 삭제 버튼을 누르고 관리자 비번을 입력하면 됩니다.

## 사진

`irotomo-board-photos` 폴더에 쌓입니다. 업로드할 때 브라우저에서 가로세로 1400px 이하로 줄이고 JPEG 로 압축하므로 한 장에 150~250KB 정도입니다.

## 메일 발송 한도

일반 지메일 계정은 하루 100통까지입니다. 인증코드 한 통 + 새 글 알림 한 통이 나가므로, 하루 40~50건 정도의 글까지 여유가 있습니다.

---

# 알아두실 점

- **코드를 고친 뒤에는 반드시 재배포해야 합니다.** 배포 → 배포 관리 → 편집(연필) → 버전 «새 버전» → 배포. 주소는 그대로 유지됩니다.
- 메인 페이지와 게시판의 첫 로딩이 1~2초 걸립니다. Apps Script 특성입니다. 메인 페이지는 기본 문구가 먼저 보이므로 체감은 덜합니다.
- 게시판 글이 200개를 넘어가면 목록이 느려집니다. 그때 오래된 글을 `hidden` 처리해 주세요.
- 비밀번호 확인은 서버에서 하므로 페이지 소스에 남지 않습니다. 다만 웹앱 주소는 공개 파일에 들어 있으니, **비밀번호를 아는 사람만 막아주는 수준**입니다. 임시 비번 3707은 되도록 바꿔 쓰시길 권합니다.
- 게시판 글쓴이의 이메일과 삭제 비밀번호는 시트에만 저장되고 사이트로는 내려가지 않습니다. 비밀번호는 해시로 저장되어 시트에서도 원문을 볼 수 없습니다.
